void element: not need a closing tag
<link> -> void element
<link rel="stylesheet" href="buttons.css"> 	-> rel: relation

special charactor: html entity

div: division

fr: free space

 The "title" attribute is an attribute that can be added to HTML elements to provide additional information about the element. When you hover over an element with a "title" attribute, the browser typically displays a small tooltip with the text from the "title" attribute.

Q: why my image doesn't show up in React project?
A: If you are using Create React App, make sure the image is placed in the public folder, and you are referencing it correctly. The public folder is served as the root, so your image path should be relative to this folder.